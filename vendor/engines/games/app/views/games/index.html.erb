<% content_for :body_content_left do %>
  <ul id="games">
    <% @games.each do |game| %>
      <li>
        <%= link_to game.title, game_url(game) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render :partial => "/shared/content_page" %>
